{
  "name": "guild",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/terror/guild.git",
  "author": "Liam <terroridk@gmail.com>",
  "license": "MIT",
  "engines": {
    "node": "15.x"
  },
  "scripts": {
    "build": "concurrently \"cd client && yarn build\" \"cd server && yarn build\"",
    "install": "(cd client && yarn install) && (cd server && yarn install)",
    "start": "node backend/dist"
  },
  "dependencies": {
    "concurrently": "^5.3.0"
  }
}
